# Contributing to EZ Studios Core

Bem-vindo ao coração do desenvolvimento procedural. Estamos construindo o futuro da criação de mundos agnósticos e sua contribuição é essencial.

## 🚀 Como Começar

1. Fork o repositório.
2. Crie sua Feature Branch (`git checkout -b feature/AmazingFeature`).
3. Commit suas mudanças (`git commit -m 'Add: AmazingFeature'`).
4. Push para a Branch (`git push origin feature/AmazingFeature`).
5. Abra um Pull Request.

## 📐 Padrões Técnicos

- **TypeScript**: Use tipos explícitos. Evite `any` a todo custo.
- **Arquitetura**: Siga o padrão de Adaptadores (`IEngineAdapter`) para novas engine targets.
- **Documentação**: Atualize os arquivos em `docs/` se houver mudança na lógica core.

## 🧪 Testes

Documente como você verificou sua mudança (ex: screenshots do preview Three.js ou código Luau gerado).

---
**EZ Studios - Entropia Zero, Criatividade Infinita.**
